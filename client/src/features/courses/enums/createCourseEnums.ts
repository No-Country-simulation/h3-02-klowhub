export enum CreateCourseTriggers {
  GENERAL = 'general',
  DETAILS = 'details',
  MODULES = 'modules',
  PROMOTION = 'promotion',
}

export enum CourseMonetizable {
  PAYMENT = 'payment',
  FREE = 'free',
}

export enum CourseType {
  LESSON = 'lesson',
  COURSE = 'course',
}
