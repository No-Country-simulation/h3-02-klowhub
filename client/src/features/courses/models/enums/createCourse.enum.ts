export enum CreateCourseTriggers {
  GENERAL = 'general',
  DETAILS = 'details',
  MODULES = 'modules',
  PROMOTION = 'promotion',
  KEY = 'activeStep',
}

export enum CourseMonetizable {
  PAYMENT = 'payment',
  FREE = 'free',
}

export enum CourseType {
  LESSON = 'lesson',
  COURSE = 'course',
}

export enum CoursePlatform {
  POWER_APPS = 'Power Apps',
  APP_SHEET = 'App Sheet',
}

export enum CourseCompetence {
  BASIC = 'basic',
  INTERMEDIATE = 'intermediate',
}
