import { Inter } from 'next/font/google';

// Fuenta optimizada con nextjs
export const inter = Inter({
  subsets: ['latin'],
  variable: '--font-open-sans',
  style: ['normal'],
  weight: ['400', '500', '600', '700'],
});
